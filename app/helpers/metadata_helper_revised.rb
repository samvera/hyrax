require "ruby-debug"
module MetadataHelper
  
  def inline_editable_metadata(resource, datastream_name, field_key, opts={})
  end
  
  
  def metadata_field_info(resource, datastream_name, field_key, opts={})
  end
  
end