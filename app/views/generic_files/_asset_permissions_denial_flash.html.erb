The file(s)
<% file_sets.first(20).each  do |fs|%>
<%= link_to(fs, file_set_url(fs)) %><%= ',' unless fs == file_sets.last %>
<% end %>
<%= '...' if file_sets.length > 20 %>
 could not be updated.  You do not have sufficient privileges to edit it.
