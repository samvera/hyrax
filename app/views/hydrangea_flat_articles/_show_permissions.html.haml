%div
  %dl.defList
    - if editor?
      %h3 Group
      = get_data_with_label(document, "Discover Access:", 'discover_access_group_t', :default=>"private")
      = get_data_with_label(document, "Read Access:", 'read_access_group_t', :default=>"private")
      = get_data_with_label(document, "Edit Access:", 'edit_access_group_t', :default=>"private")

      %h3 Individual
      = get_data_with_label(document, "Discover Access:", 'discover_access_t', :default=>"private") 
      = get_data_with_label(document, "Read Access:", 'read_access_t', :default=>"private") 
      = get_data_with_label(document, "Edit Access:", 'edit_access_t', :default=>"private") 
