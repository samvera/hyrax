<%= f.input key,
            as: :multi_value,
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/#{f.object.model_name.plural}/location",
                      'autocomplete' => key }
            },
            required: f.object.required?(key) %>
