<% if params[:parent_id] %>
  <% f.object.in_works_ids.each do |work_id| %>
    <%= f.input :in_works_ids, as: :hidden, input_html: { value: work_id, multiple: true } %>
  <% end %>
  <%= hidden_field_tag 'parent_id', params[:parent_id] %>
<% end %>
