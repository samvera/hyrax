<%= content_tag :ul,
                id: "sortable",
                class: "list-unstyled grid clearfix",
                data: {
                  id: @change_set.id,
                  "class-name" => @change_set.model_name.plural,
                  "singular-class-name" => @change_set.model_name.singular,
                  version: @change_set.version
                } do %>
  <% @change_set.member_presenters.each do |member| %>
    <%= render "file_manager_member", node: member %>
  <% end %>
<% end %>
