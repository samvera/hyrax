<% unless visibility_errors_for(form: f).empty? %>
  <%= f.full_error(:visibility) %>
<% end %>

<% unless embargo_release_date_errors_for(form: f).empty? %>
  <%= f.full_error(:embargo_release_date) %>
<% end %>

<% unless visibility_after_embargo_errors_for(form: f).empty? %>
  <%= f.full_error(:visibility_after_embargo) %>
<% end %>

<% unless lease_expiration_date_errors_for(form: f).empty? %>
  <%= f.full_error(:lease_expiration_date) %>
<% end %>

<% unless visibility_after_lease_errors_for(form: f).empty? %>
  <%= f.full_error(:visibility_after_lease) %>
<% end %>
