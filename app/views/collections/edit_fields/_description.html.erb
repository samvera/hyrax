<div>
  <%= f.text_area :title, name: "#{ActiveModel::Naming.singular(f.object)}[#{key.to_s}]", id: "#{ActiveModel::Naming.singular(f.object)}_#{key.to_s}", value: v, class: "input-large", required: record.required?(key)  %>
  <%= render "records/edit_fields/suffix", key: key, index: index, single: true %>
</div>
